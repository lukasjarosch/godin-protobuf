language: go
sudo: required

go:
  - 1.12.x

services:
  - docker

install:
  - docker pull uber/prototool:latest

script:
  - ./scripts/prototool.sh generate
  - sudo chown -R travis: gen
  - ./scripts/push.sh
  - ls -la
  - ls -la gen
  - ls -la gen/go
